<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "List", tickets_path  %></li>
      <li><%= link_to "New Story", new_story_path %></li>
      <li><%= link_to "New Task", new_task_path %></li>
      <li><%= link_to "New Bug", new_bug_path %></li>
    </ul>
  </div>          
  <div class="content">          
    <h2 class="title">All Tickets</h2>
    <div class="inner">
      <table class="table">
        <tr>             
          <th class="first">ID</th>
          <th>Title</th>
          <th>Type</th>
          <th class="last">Created at</th>
        </tr>
        <% @tickets.each do |ticket| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= ticket.id %>
          </td>
          <td>
            <%= link_to ticket.title, edit_ticket_path_by_type(ticket) %>
          </td>
          <td>
            <%= ticket.type %>
          </td>
          <td class="last">
            <%= ticket.created_at %>
          </td>
        </tr>          
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        <%= paginate @tickets, :outer_window => 1%>
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
